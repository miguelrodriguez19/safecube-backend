# .github/workflows/release-main.yml
name: 'Release & Deploy (main)'

on:
  push:
    branches: [ main ]

concurrency:
  group: main-release
  cancel-in-progress: false

jobs:

  ###########################################################################
  # CI (lite): build + tests + version guard                                #
  ###########################################################################
  ci-lite:
    name: 'CI (lite)'
    permissions:
      contents: read
      pull-requests: read
      security-events: write

    uses: ./.github/workflows/ci-java-reusable.yml
    with:
      run-version-check: true
      run-security-scan: false
      run-code-scan: false
      run-acceptance-tests: false
      run-mutation-tests: false


  ###########################################################################
  # Release: Git tag + Docker build & push (GHCR)                           #
  ###########################################################################
  release:
    name: 'Release artifacts'
    needs: ci-lite
    runs-on: ubuntu-latest
    permissions:
      contents: write      # allow git tag push
      packages: write      # allow GHCR push

    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Set up Java 21 (+ Maven cache)
        uses: ./.github/actions/java-steps
        with:
          java-version: 21

      - name: Read and clean version from pom.xml
        id: version
        run: |
          RAW_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          CLEAN_VERSION=${RAW_VERSION%-SNAPSHOT}
          echo "version=$CLEAN_VERSION" >> "$GITHUB_OUTPUT"

      - name: Create Git tag
        run: |
          git tag "v${{ steps.version.outputs.version }}"
          git push origin "v${{ steps.version.outputs.version }}"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Download executable JAR
        uses: actions/download-artifact@v4
        with:
          name: executable-jar
          path: target

      - name: Build & push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:${{ steps.version.outputs.version }}
            ghcr.io/${{ github.repository }}:latest


  ###########################################################################
  # Deploy: manual approval → Koyeb                                         #
  ###########################################################################
  deploy-production:
    name: 'Deploy to Koyeb'
    needs: release
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Install Koyeb CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/koyeb/koyeb-cli/master/install.sh | sh
          echo "$HOME/.koyeb/bin" >> $GITHUB_PATH

      - name: Authenticate Koyeb CLI (non-interactive)
        run: |
          mkdir -p ~/.koyeb
          echo "c: ${{ secrets.KOYEB_API_TOKEN }}" > ~/.koyeb.yaml

      - name: Trigger redeploy on Koyeb
        run: koyeb service update selective-charil/safecube-backend \
          --docker ghcr.io/${{ github.repository }}:latest \
          --deployment-strategy immediate


